# Database
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=app-outlet
spring.data.mongodb.repositories.type=auto

# Synchronization
appoutlet.synchronization.cron=0 * * * * *
appoutlet.synchronization.enable=false
appoutlet.synchronization.snap-store.enable=false
appoutlet.synchronization.app-image-hub.enable=false
appoutlet.synchronization.flathub.enable=false

# Spring docs
springdoc.swagger-ui.path=docs
